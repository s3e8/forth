\ simple sampler







add-sample

\ todo: if one word isnt defined, go to def... mode is where we assume all are defined... reorder, and notify if def cant be found

init-sample... with some length? uses temp float stack
create/save-sample mallocs at a given length




compressor where after surpassed can put your own sound in? like play sin...